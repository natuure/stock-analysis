# 데이터 파이프라인 — Python 스크립트·수집 로직·데이터 구조

## Python 스크립트

### 뉴스분석.py
```bash
python 뉴스분석.py   # 장마감 후 실행. 오늘 날짜 기준 KRX 전종목 자동 수집
```
- FinanceDataReader(`fdr.StockListing('KRX')`)로 전종목 시세 1회 호출 (KONEX 제외)
- 거래대금 상위 50 / 등락률 상위 50(거래대금 300억↑ 대상) 산출 — **KIS 통합(KRX+NXT)
  데이터로 보강** (2026-06-21 도입, 아래 "KIS 통합 거래대금·등락률 보강" 참고)
- 코스피·코스닥 지수 종가/등락률도 같이 수집 (`fetch_indices()`, `fdr.DataReader('KS11'|'KQ11')`)
- MongoDB `stock_data` 컬렉션에 저장 (웹앱 달력 초록 점 자동 표시)
- 거래대금+등락률 종목(최대 100개)의 토스증권 일봉 캔들 85개를 미리 조회해 MongoDB `candles`에 캐싱
  (토스 API는 IP 허용 목록 기반이라 고정 IP인 로컬에서만 호출, Vercel은 직접 호출하지 않음.
  2026-06-20부터는 `api/candles.js`가 KIS를 우선 호출하므로, 이 캐싱은 KIS 장애 시에만 쓰이는
  폴백 데이터가 됨 — 자세한 내용은 아래 "KIS 일별 캔들 조회(api/candles.js)" 참고)
- `분석결과/뉴스데이터_YYYYMMDD.json` 생성
- Naver 뉴스 쿼리: `{name} 특징주`, `{name} 급등 이유`, `{name} 상승 배경` 등 8개
- 날짜 기준 검색: 파일날짜 ~ 오늘 (3일 이상 지난 경우 파일날짜+3일로 제한)
- 당일 기사 우선 정렬

### 저장분석.py
```bash
python 저장분석.py                              # 분석결과/ 최신 분석결과_*.json 자동 탐색
python 저장분석.py "분석결과/분석결과_2026-06-17.json"  # 파일 직접 지정
```
- Claude Code가 생성한 분석 JSON을 MongoDB `ai_analysis`에 저장

### 주간분석.py (2026-06-21 도입, 2026-06-21 범위 축소)
```bash
python 주간분석.py   # 아무 때나 실행 가능 (장마감 후가 자연스러움)
```
- `fdr.DataReader('KS11'|'KQ11')`로 최근 21일치(이번 주 + 비교 기준 지난 주 확보용 여유)만
  받아서 **가장 최근 1주(월~금)치만** 계산 — 과거 주차는 다시 계산하지 않음(이미 저장된
  과거 주는 그대로 유지됨)
- `monday_of(today)`로 이번 주 월요일을 구하고, 그 주 마지막 거래일 종가 vs 지난 주
  마지막 거래일(보통 금) 종가를 비교해 `{close, change, changeRate}` 계산. 이번 주에
  아직 거래일이 없으면(주말·휴일에 실행 등) 한 주 전으로 자동 이동
- 주차 키는 `f'{d.year}-W{iso_week}'` 형태(예: `2026-W25`) — `src/utils.js`의
  (현재는 `Calendar.jsx` 내부에 로컬로 있는) ISO 주차 계산과 동일한 규칙이어야
  웹앱이 키를 매칭할 수 있음. **숫자가 아닌 문자열이라 사전식 정렬로는 시간순이
  안 됨** (`"2026-W9"` > `"2026-W25"`)
- MongoDB `weekly_indices` 컬렉션에 해당 주차 1건만 upsert
- 웹앱 `/api/getData`가 이 컬렉션 전체를 같이 읽어 `weeklyIndices`로 내려주고,
  달력의 **주차(W##) 칸**(2026-06-22부터 — 그 전에는 토요일 칸이었음)이 그 주(월~금)의
  코스피/코스닥 변동률을 표시함(`Calendar.jsx`가 그 주 월요일 날짜로 주차를 계산해 매칭)
- 처음 도입 시점에 과거 ~76주를 1회 백필해둔 상태라 과거 달도 정상 표시됨. 더 과거
  주차가 비어있으면 `LOOKBACK_DAYS`를 늘려 그 주만 따로 1회 실행해 백필하면 됨.

### 종목분석.py (2026-06-22 도입, 2026-06-24 재무제표 전용으로 범위 축소, 2026-06-25 KIS 현재가 +
### MongoDB 저장으로 웹앱 "종목 분석" 탭과 자동 연동)
```bash
python 종목분석.py 종목명
# 예) python 종목분석.py 인지컨트롤스
```
- 단일 종목의 DART 재무제표 + KIS 현재가/시가총액/발행주식수를 받아오는 데이터 수집 스크립트.
  밸류에이션 계산·공시 목록·네이버 뉴스/웹 검색·기업 개요는 이 스크립트가 하지 않음(2026-06-24
  제거) — 사업 개요, 임원/최대주주 상세, SWOT·최종 투자의견, `.docx` 리포트 생성은 이 결과
  JSON과 dart-mcp/웹 검색을 보고 Claude Code가 직접 작성한다. 메인 화면의 "종목 분석" 탭
  기업개요 버튼이 이 출력 구조를 읽어 PER/PBR/ROE/EV·EBITDA와 적정주가를 계산한다
  (`CompanyOverviewView.jsx`). 실행이 끝나면 결과를 MongoDB `company_analysis` 컬렉션에도
  `_id=종목코드`로 upsert 저장하므로(2026-06-25), 수동 복사 없이 웹앱 검색창에 종목명을
  입력(자동완성 지원)하면 `/api/getCompanyOverview`가 바로 그 문서를 읽어 표시한다.
- DART Open API(`opendart.fss.or.kr`)를 **직접** 호출 — `.env.local`의 `DART_API_KEY` 필요
  (무료 발급, [opendart.fss.or.kr](https://opendart.fss.or.kr)). 대화 중에만 쓸 수 있는
  `dart-mcp` MCP 도구와는 별개이며, 그 도구가 내부적으로 감싸고 있는 것과 같은 공식 API를
  스크립트가 직접 호출하는 구조.
- **corp_code 매핑**: DART API는 종목명이 아니라 8자리 `corp_code`로 조회해야 해서, 최초
  실행 시 `corpCode.xml`(전체 상장사 목록)을 받아 `_dart_corp_codes.json`으로 캐싱(`.gitignore`
  처리됨, git 추적 안 함). 이후 실행은 이 캐시로 종목명→corp_code를 찾음 — `find_corp_code()`:
  1. 대소문자까지 정확히 일치 2. 대소문자만 다른 정확 일치(예: `sk하이닉스` 입력 → `SK하이닉스`)
  3. 그래도 없으면 부분일치, 후보가 여럿이면 입력값과 길이 차이가 가장 작은 회사명을 선택.
  **2단계(대소문자 무시 정확 일치)를 부분일치보다 먼저 보는 이유**(2026-06-25 버그 수정,
  [HISTORY.md](HISTORY.md) 참고): 부분일치만 썼을 때 `sk하이닉스` 입력이 무관한 짧은 회사명
  `이닉스`(종목코드 452400)에 먼저 매칭되고 실제 `SK하이닉스`(000660)는 매칭되지 않는 사고가
  있었음 — 짧은 회사명이 입력 문자열에 우연히 부분문자열로 포함된 경우였음.
- **재무 데이터**: `fnlttSinglAcntAll.json`(단일회사 전체 재무제표)를 다음 순서로 가져옴
  (`find_latest_report()`/`build_fetch_plan()`):
  1. `find_latest_report()`가 오늘 날짜 기준 **실제 제출된 가장 최근 보고서**(연도 Y, 종류)를
     찾는다 — 사업보고서가 같은 '연도' 라벨이라도 실제로는 다음 해 3월에 제출되어 그 해
     1~3분기보고서보다 항상 늦으므로, 탐색 순서는 사업보고서→3분기→반기→1분기(연도
     내림차순으로 실제 DART 응답이 있는 첫 보고서를 채택).
  2. `build_fetch_plan()`이 그 결과에 따라 조회 범위를 정함:
     - 최신이 Y년 **1분기보고서** → Y-3,Y-2,Y-1년 사업보고서 + Y년 1분기보고서
     - 최신이 Y년 **반기보고서** → Y-3,Y-2,Y-1년 사업보고서 + Y년 1분기·반기보고서
     - 최신이 Y년 **3분기보고서** → Y-3,Y-2,Y-1년 사업보고서 + Y년 1분기·반기·3분기보고서
     - 최신이 Y년 **사업보고서**(연간 확정) → Y-3,Y-2,Y-1,Y년 사업보고서 4개년(분기 조회 불필요)
  - 연결(`CFS`) 기준, 없으면 별도(`OFS`)로 폴백.
  - **계정명이 보고서·기업마다 다름**(직접 확인): 사업보고서는 `당기순이익`, 분기·반기보고서는
    `분기순이익`/`반기순이익`을 쓰는 회사가 있어 `NET_INCOME_NAMES`에 후보를 다 나열해 찾음.
    화신은 '이익' 대신 '손익'을 써서 `당기순손익`으로 보고함(음수도 가능하다는 의미로 보임,
    직접 확인 2026-06-25) — 같은 회사의 분기보고서는 영업이익도 `영업손익`으로 써서
    `OPERATING_INCOME_NAMES`에 따로 후보를 둠. 지배주주지분도 `지배기업소유주지분`/
    `지배기업 소유주지분`(공백 차이)/`지배기업의 소유주에게 귀속되는 자본`/`지배기업의
    소유주지분`/`지배기업의 소유지분`(노바렉스·SK하이닉스에서 직접 확인, 2026-06-25) 다섯
    가지가 다 쓰여서 `PARENT_EQUITY_NAMES`로 같이 대응. **`지배기업 소유주지분`(공백 있는
    표기)은 재무상태표(BS) 외에 포괄손익계산서(CIS)의 총포괄손익 중 지배기업 귀속분(흐름)
    이름으로도 쓰여서 더 위험함** — 노바렉스에서 실제로 BS의 진짜 지배주주지분(약 2,002억)이
    아니라 CIS의 동명 항목(약 220억, 전혀 다른 의미)이 먼저 매칭되는 버그가 났었음(다른
    필드들과 달리 `extract_account()`가 그동안 `sj_div`를 보지 않고 이름만 봤기 때문). 이제
    `extract_account()`에 선택적 `sj_div` 인자를 추가해 지배주주지분 추출 시 `sj_div='BS'`로
    한정하도록 수정 — 비슷하게 이름이 겹치는 다른 계정이 또 나오면 같은 방식으로 대응.
  - **매출액**도 `매출액`/`영업수익` 둘 다 없이 `수익(매출액)`만 쓰는 회사가 있음(일지테크
    2025년 사업보고서에서 직접 확인, 2026-06-25 — 영업이익은 정상 추출됐는데 매출액만 `null`로
    나와서 발견). 달바글로벌은 한 술 더 떠 2024년 사업보고서엔 '액' 자체를 빼고 `매출`만 쓰고
    2025년엔 `매출액`을 씀 — 같은 회사도 보고연도에 따라 표기가 바뀜(직접 확인, 2026-06-25).
    `REVENUE_NAMES`로 후보를 같이 대응.
  - **차입금**(EV 계산 등 리포트 작성 시 참고용)은 재무상태표(`sj_div='BS'`)로 한정해서 합산함
    — 현금흐름표에도 `차입금의 차입`/`차입금의 상환` 같은 유사한 이름의 **흐름(flow)** 항목이
    있어, 필터 없이 부분일치만 하면 잔액(stock)과 흐름이 섞여 실제 자산총계보다 큰 말도 안
    되는 차입금이 나왔던 버그가 있었음(직접 검증 후 수정).
  - **기본 주당이익**은 DART가 직접 보고하는 값(가중평균주식수 반영이라 `당기순이익/발행주식수`
    보다 정확)을 그대로 저장 — EPS 계산은 더 이상 스크립트가 하지 않고 이 값을 보고 Claude
    Code가 필요시 계산. `EPS_NAMES` 상수로 연간·분기 양쪽에서 공통 추출(2026-06-25, 추세
    차트의 올해 분기 시점 EPS도 보여주려고 분기 쪽에도 추가). 계정명이 공백·`(손실)` 접미사
    유무로 4가지(`기본 주당이익`/`기본주당이익`/`기본 주당이익(손실)`/`기본주당이익(손실)`,
    마지막은 일지테크에서 직접 확인)에 더해, 분기보고서는 당기순이익처럼 `기본주당분기순이익`을
    따로 쓰는 회사도 있음(SK하이닉스 2026년 1분기보고서에서 직접 확인) — 반기보고서의
    `기본주당반기순이익`은 같은 패턴으로 추정되나 아직 직접 확인 사례가 없어 후보에 포함 안 함.
    달바글로벌은 '주당이익'이 아니라 어순이 다른 `기본주당순이익`/`기본주당순이익(손실)`
    ('이익' 앞에 '순' 추가)을 연간·분기 모두 일관되게 씀(직접 확인, 2026-06-25) — 이것도
    후보에 포함. **카카오처럼 그 기간에 중단영업이 있었던 회사는 단일 합계줄 없이 `계속영업
    기본주당순이익`+`중단영업 기본주당순이익`으로 나눠서만 보고함**(2025년 사업보고서·2026년
    1분기보고서 둘 다에서 직접 확인) — `extract_eps()`가 `EPS_NAMES`로 못 찾으면 이 두 계정을
    찾아 합산하는 폴백을 별도로 둠(계속영업 값만 있고 중단영업 계정 자체가 없으면 그 회사는
    중단영업이 없었다는 뜻이라 0으로 처리). 그래도 못 찾으면(공시 자체가 없는 회사도 있음,
    직접 확인) `null`로 남음, 정상 동작.
  - **유형자산**도 일지테크 2025년 사업보고서엔 `유형자산` 라인 자체가 없고 `기초 유형자산`
    계정만 있음(직접 확인, 2026-06-25). 그 행의 전기(`frmtrm_amount`)·전전기(`bfefrmtrm_amount`)
    값이 각각 2024·2023년 사업보고서의 실제 `유형자산` 당기 값과 정확히 일치함을 대조 확인한 뒤
    `FIXED_ASSET_NAMES`(`['유형자산', '기초 유형자산']`)에 후보로 추가 — 이름은 "기초"(기간
    시작 시점)지만 실질적으로 그 해의 유형자산 BS 라인 역할을 하는 것으로 판단함. 다른 회사·
    연도에서 진짜 기초/기말이 둘 다 따로 보고되면 어느 쪽이 먼저 매칭될지 보장되지 않는 한계가
    있음(`extract_account()`는 candidate 목록 순서가 아니라 API 응답에 나온 순서로 첫 매칭을
    반환). 이런 식으로 못 찾으면(드물게 공시 자체가 없는 회사도 있음) 항목별로 `null`로 남음,
    정상 동작.
  - **재무상태표 세부 항목**(2026-06-24 추가, 종목 분석 탭 재무상태표 버튼용): 무형자산·
    유형자산·재고자산·매출채권·단기금융상품·선수금·자본금·자본잉여금·이익잉여금을
    `annual_financials`와 `quarterly_financials` 양쪽 모두에 추가(분기말 시점 스냅샷도 같은
    구조로 보고되므로 동일하게 추출). CAPEX 비중·재고자산 비중 등 비율 계산은 스크립트가
    하지 않고 이 원자료를 보고 화면/Claude Code가 계산.
    - **매출채권·재고자산도 회사·연도별로 계정명이 다름**(직접 확인): `매출채권` 외에
      `매출채권및기타채권`/`매출채권 및 기타채권`/`매출채권 및 기타유동채권`이 쓰이고
      (`ACCOUNTS_RECEIVABLE_NAMES`), 같은 회사도 보고연도에 따라 표기가 바뀔 수 있음(인지
      컨트롤스: 2023년 `매출채권 및 기타유동채권` → 2025년 `매출채권`). 재고자산도
      `재고자산`/`유동재고자산` 두 가지(`INVENTORY_NAMES`). 장기성매출채권(비유동)은
      의도적으로 후보에서 제외(단기/장기가 섞이면 비중 계산이 왜곡됨).
    - **자본잉여금**도 회사마다 다름: 삼성전자처럼 `자본잉여금` 합계줄 자체를 안 쓰고 세부
      항목인 `주식발행초과금`만 보고하는 경우가 있어 `CAPITAL_SURPLUS_NAMES`로 같이 대응.
    - **선수금**은 2018년 수익인식기준(K-IFRS 1115) 도입 이후 `계약부채`로 대체 표기하는
      회사가 있어 `ADVANCE_RECEIPTS_NAMES`로 같이 찾음 — 그래도 회사에 따라 둘 다 없으면(해당
      거래 자체가 없는 경우) `null`로 남음, 정상 동작.
    - **단기금융상품**도 보유하지 않는 회사는 계정 자체가 없어 `null`로 남음(정상 동작).
- **현재가·시가총액·발행주식수**(2026-06-25 추가, "종목 분석" 탭 기업개요 버튼의 PER/PBR/
  EV·EBITDA 계산용): DART에는 없는 데이터라 KIS Open API `inquire-price`(FHKST01010100,
  `get_kis_token()`/`fetch_kis_quote()`)를 별도로 호출 — `뉴스분석.py`/`api/candles.js`와
  같은 `FID_COND_MRKT_DIV_CODE=UN`(KRX+NXT 통합) 모드를 써서 NXT 체결분이 반영된 진짜
  현재가·거래대금을 받는다(직접 검증, 2026-06-25: 삼성전자 KRX단독 340,500원/거래대금
  15.8조 vs 통합 339,500원/27.4조). 결과는 `quote` 키에 `{price, marketCap_억원,
  sharesOutstanding}`로 저장. **시가총액은 KIS가 주는 `hts_avls` 필드를 안 쓰고 현재가×
  상장주식수로 직접 계산**함 — `hts_avls`를 공식 문서 표기대로(백만원) 해석해서 변환하면
  직접 계산한 값과 100배 차이가 나는 걸 발견해서(2026-06-25), 단위가 불확실한 필드 대신
  이미 정확히 아는 두 값으로 직접 계산. 종목분석.py는 (회사 결과 저장과는 별개로) KIS 토큰을
  캐싱하지 않고 매 실행마다 새로 발급함 — 수동으로 가끔 실행하는 스크립트라 1분당 1회 발급
  제한에 거의 안 걸림(뉴스분석.py처럼 매일 자동 실행되는 스크립트만 `kis_token` 컬렉션으로
  캐싱). `KIS_APP_KEY`/`KIS_APP_SECRET` 없으면 경고만 출력하고 `quote: null`로 계속 진행
  (DART 재무제표 수집은 영향 없음).
- 결과: `종목분석결과/{종목명}_{YYYYMMDD}.json` 저장(`.gitignore` 처리됨) — `quote`(현재가·
  시가총액·발행주식수), `annual_financials`(연간 사업보고서 3~4개년), `quarterly_financials`
  (올해 진행 분기/반기), `latest_report`(최신 보고서 연도·종류) 필드. **같은 내용을 MongoDB
  `company_analysis` 컬렉션에도 `_id=종목코드`로 upsert 저장**(2026-06-25 추가) — `MONGODB_URI`가
  `.env.local`에 없으면 이 단계만 경고와 함께 건너뛰고 로컬 JSON 저장은 그대로 진행됨. 이
  MongoDB 저장 덕분에 웹앱 "종목 분석" 탭 검색창에서 종목명을 입력하면 별도 수동 작업 없이
  바로 조회됨(`api/getCompanyOverview.js`, [ARCHITECTURE.md](ARCHITECTURE.md) 참고).
- **이 스크립트가 수집하지 않는 것**(2026-06-24부터, 의도적 축소): 밸류에이션 비율 계산
  (PER·PBR·EV/EBITDA·적정주가 등 — 원자료(`quote`+재무제표)만 주고 계산은 화면/Claude Code가
  함), TTM 순이익, 공시 목록, 네이버 뉴스/웹 검색, 기업 개요·사업의 개요·임원/최대주주 현황 —
  리포트 작성 시 dart-mcp나 웹 검색으로 직접 보완 필요.

---

## 데이터 수집 (FinanceDataReader)

`fdr.StockListing('KRX')` 1회 호출로 KRX 전종목(KOSPI/KOSDAQ, KONEX 제외)의
`Code, Name, Market, Close, Changes, ChagesRatio, Volume, Amount, Marcap`을 가져옴.
- **거래대금 상위 50**: `Amount` 내림차순
- **등락률 상위 50**: `Amount >= 300억` 필터 후 `ChagesRatio` 내림차순
- **상한가 판정**: `changeRate >= 29.5` (`isUpperLimit: true`)
- **전일 순위(prevRank)**: MongoDB에서 직전 거래일 `stock_data` 문서를 조회해 종목코드로 매칭 (없으면 NEW)
- **체결강도**: FDR로 계산 불가하여 컬럼 자체 제거됨 (HTS 엑셀 시절에는 있었음)
- **ETF/ETN/스팩 제외** (2026-06-21): `fetch_market_data()`에서 이름에 `스팩|기업인수목적`이
  들어간 행을 제외함. ETF/ETN은 직접 확인해본 결과 `fdr.StockListing('KRX')` 자체가 보통주/
  우선주만 반환해서(KODEX 200 등 주요 ETF 코드도 안 잡힘) 별도 필터가 필요 없었음 — 스팩만
  실제로 섞여 있었음(`Dept` 컬럼의 `SPAC(소속부없음)` 태그만으론 전부 안 잡혀서 이름 패턴
  사용, 관리종목으로 전환된 스팩까지 잡아냄). 기존 MongoDB `stock_data`/`ai_analysis`에 이미
  들어가 있던 스팩 1건(2026-06-19, 메리츠제2호스팩)도 같이 제거 + rank 재정렬함.

> ✅ **해결됨 — NXT(대체거래소) 거래량 포함 여부** (2026-06-19 미확인 → 2026-06-21 해결):
> FDR `StockListing('KRX')`는 NXT 체결분을 포함하지 않는 KRX 단독 수치임을 KIS Open API로
> 직접 확인했다(`FID_COND_MRKT_DIV_CODE`를 `J`/`NX`/`UN`으로 바꿔 같은 종목·날짜를 비교,
> `UN`=`J`+`NX` 정확히 일치). FDR 자체를 고칠 수는 없지만, 거래대금·등락률 상위 50을
> KIS 통합(UN) 데이터로 재보강하는 방식으로 해결함 — 아래 "KIS 통합(KRX+NXT) 거래대금·
> 등락률 보강" 참고.

### 토스증권 캔들 캐싱
- `fetch_candles(token, code, date_str)`로 종목당 1회, `interval=1d&count=85` 조회 (`before` 커서로 해당 날짜 지정)
- 85개를 가져오는 이유: 차트엔 최근 60거래일만 표시하지만, 20일선이 표시 구간 맨 왼쪽까지 끊김 없이
  그려지려면 19거래일치 선행 데이터가 더 필요해서 (`StockChartPanel.jsx`의 `VISIBLE_COUNT=60` 참고)
- Rate Limit(burst 5, 초당 1개 충전) 대비 종목당 1.1초 슬립

### 60일 신고가 대비 등락률 (`high60Rate`, 2026-06-21 도입)
- 거래대금·등락률 표의 "거래량" 열을 대체. `cache_candles()`가 이미 받아온 캔들(종목당 85개)에서
  첫 60개(`HIGH60_WINDOW`)의 **종가**(`closePrice`) 중 최고값을 "60일 신고가"로 보고
  `(현재가-60일최고가)/60일최고가*100`을 계산 — 캔들 캐싱과 같은 호출로 처리되어 API 추가
  호출이 없다. intraday 고가(`highPrice`)가 아니라 종가 기준(2026-06-21 결정 — 일시적
  꼬리(wick)를 신고가로 치지 않기 위함).
- 0%를 상한으로 클램프(현재가가 60일 신고가 이상이면 0%) — **토스 캔들의 종가는 KRX 단독이
  아니라 KIS UN(NXT 포함 통합가)과 일치함을 직접 확인**(2026-06-21, 000660의 2026-06-18
  종가가 토스·KIS UN 모두 2,699,000으로 일치, 같은 날 FDR만 KRX 단독인 2,685,000 — FDR이
  아니라 토스도 KRX 단독이라고 잘못 적어뒀던 걸 정정함). 그래도 토스와 KIS 간 반영 시점이
  미세하게 어긋날 가능성에 대비해 클램프는 유지.
- 토스 캔들 fetch 실패 종목은 `high60Rate: null`(프론트에서 '-' 표시).
- `cache_candles(vol, rate, date)` 호출을 `save_to_mongodb(...)`보다 먼저 하도록 `main()` 순서를
  바꿔서, 계산된 `high60Rate`가 vol/rate 딕셔너리에 채워진 뒤 저장되게 함.
- 과거 06-15~19도 1회성 스크립트로 백필함(2026-06-21) — 그 날짜들에 이미 캐싱돼 있던 `candles`
  컬렉션(해당 날짜 기준 85개)을 그대로 재사용해 API 호출 없이 계산. 종목별로 그 날짜의 캔들이
  캐싱 안 돼 있던 소수(06-15 1개, 06-17 2개)는 `high60Rate: null`로 남음.
- 계산 기준을 intraday 고가에서 종가로 바꾼 뒤(2026-06-21) 06-15~19를 같은 방식으로 재백필함
  — 필드 구조는 안 바뀌었지만 값 자체가 달라진 콘텐츠 변경이라, 스팩 제거·등락률 기준선
  수정 때와 동일한 이유로 `CACHE_VERSION`을 한 번 더 올렸다(7→8).

### KIS 캔들 조회 — 일봉/주봉 (`api/candles.js`, 2026-06-20 도입, 주봉은 같은 날 추가)
- 종목 클릭 시 Vercel이 **KIS(한국투자증권) Open API를 실시간으로 직접 호출**한다.
  토스와 달리 KIS는 IP 허용 목록 제한이 없음을 확인했음 (Vercel 프리뷰 배포에서 토큰 발급·조회
  성공으로 검증, 2026-06-20).
- 엔드포인트: `GET /uapi/domestic-stock/v1/quotations/inquire-daily-itemchartprice`
  (`tr_id: FHKST03010100`), 시작일~종료일 범위로 한 번에 최대 100개 캔들 반환. 같은 엔드포인트를
  `FID_PERIOD_DIV_CODE`로 일봉(`D`)/주봉(`W`) 전환해서 재사용한다.
- 프론트엔드가 보내는 `?period=D|W` 쿼리에 따라 `PERIOD_CONFIG`에서 조회 개수·범위를 다르게 잡음:
  - `D`(일봉): 캔들 85개, `FID_INPUT_DATE_1`은 대상일 기준 135캘린더일 전(85거래일 확보용 여유)
  - `W`(주봉): 캔들 75개, `FID_INPUT_DATE_1`은 대상일 기준 540캘린더일 전(75주 확보용 여유)
  - 둘 다 `FID_INPUT_DATE_2`는 대상일
- 접근토큰은 `/oauth2/tokenP`로 발급하며 **1분당 1회 발급 제한**이 있어 MongoDB `kis_token`
  컬렉션에 캐싱 후 만료(`expires_in`, 보통 24시간) 전까지 재사용한다.
- 응답(`output2`)의 `stck_bsop_date/stck_oprc/stck_hgpr/stck_lwpr/stck_clpr/acml_vol`을
  프론트엔드가 기대하는 `{timestamp, openPrice, highPrice, lowPrice, closePrice, volume}` 형태로
  변환하고, 날짜 내림차순(최신순)으로 정렬해 `PERIOD_CONFIG`의 개수만큼 잘라서 반환한다.
- KIS 호출이 실패하면(네트워크 오류, 상장폐지 등) **일봉(`D`)일 때만** 기존 토스 캐시(`candles`
  컬렉션)로 폴백한다 — 이 캐시는 위 "토스증권 캔들 캐싱" 절차로 여전히 매일 채워지지만 일봉만
  보관하므로, 주봉(`W`)은 KIS 실패 시 폴백 없이 빈 배열을 반환한다.
- 이 전환으로 "그날 거래대금/등락률 상위 50에 든 종목만 캔들 조회 가능"하던 한계가 사실상
  해소됨 (KIS는 캐싱 없이 아무 종목이나 즉시 조회 가능).

### 코스피·코스닥 지수 수집
- `fetch_indices()`: 최근 7일 범위로 `KS11`(코스피)/`KQ11`(코스닥)을 조회해 마지막 두 행의 `Close`로
  `close`, `change(포인트)`, `changeRate(%)`를 직접 계산 (FDR이 포인트 변동을 안 주므로 직접 차감)
- 과거 날짜 백필 시엔 `end`를 해당 날짜로 고정한 별도 호출 필요 (스크립트엔 "오늘" 전용 버전만 있음 — 과거 날짜 백필은 1회성으로 직접 작성해 실행함, 2026-06-19 기준 6/15~18 백필 완료)

### KIS 통합(KRX+NXT) 거래대금·등락률 보강 (`뉴스분석.py`, 2026-06-21 도입)
- FDR `StockListing('KRX')`는 KRX 거래소 단독 수치라 대체거래소 NXT(넥스트레이드)에서 체결된
  거래량·거래대금·종가가 빠져 있음을 KIS로 직접 검증함: `inquire-daily-itemchartprice`의
  `FID_COND_MRKT_DIV_CODE`를 `J`(KRX만)/`NX`(NXT만)/`UN`(통합)으로 바꿔 같은 종목·날짜를
  조회하면 **`UN` = `J` + `NX`가 거래량·거래대금 모두 정확히 일치**(삼성전자로 검증).
  종가도 더 늦게 마감하는 NXT 쪽 값이 그날의 진짜 최종 종가로 나옴.
- 종목 1개당 1~2회 호출해야 해서 전종목(약 2,696개)을 다 보강하긴 비효율적(추정 26~35분) —
  대신 **2단계 필터**로 후보를 추린다(2026-06-24 재설계):
  1. **1단계(사전 후보 풀)**: `RATE_PRECHECK_MIN_AMOUNT`(거래대금 30억 이상, FDR 단독 기준,
     평소 500개 안팎)로 넓게 추려 KIS로 보강한다(`enrich_with_kis()`).
  2. **2단계(최종 컷오프)**: 보강(또는 NXT 미거래 시 FDR 폴백) 후의 **통합 거래대금**이
     `RATE_MIN_AMOUNT`(300억) 이상인 종목만 남겨서 그중 등락률 상위 50을 뽑는다
     (`build_vol_rate_from_enriched()`). 거래대금 상위 50은 이 컷오프 없이 보강된 후보
     전체에서 그대로 뽑는다(원래도 최소 거래대금 기준이 없는 "무조건 상위 50"이라 영향 없음 —
     오늘 데이터 기준 거래대금 50등 컷오프 자체가 1,113억원으로 300억보다 훨씬 높음).
  - 1단계를 `RATE_MIN_AMOUNT`(300억)가 아니라 훨씬 낮은 `RATE_PRECHECK_MIN_AMOUNT`(30억)로
    잡는 이유: FDR `Amount`는 KRX 단독 거래대금이라, NXT 비중이 큰 종목은 실제(KRX+NXT
    통합) 거래대금이 300억을 넘어도 FDR 기준으론 한참 못 미칠 수 있다 — 2026-06-24 직접
    확인(에이프릴바이오 FDR 206.6억→KIS통합 794.6억/+285%, 로켓헬스케어 FDR 40.1억→KIS통합
    370.3억/+823%, 삼천당제약 FDR 297.7억→KIS통합 520.0억/+75%). 1단계 기준에서 300억을
    그대로 쓰면 이런 종목은 KIS 보강 자체를 못 받고 등락률 상위 50에서 통째로 누락된다.
- KIS 토큰은 `api/candles.js`의 `getKisToken`과 동일한 MongoDB `kis_token` 컬렉션을
  공유해서 캐싱한다(발급 1분당 1회 제한을 Vercel 쪽과 같이 지켜야 함).
- **UN 모드의 함정**: NXT에서 전혀 거래되지 않는 종목(우선주 등 일부)은 `UN`이 빈
  `output2`를 반환함(직접 확인). 이 경우 **`J`로 재조회하지 않고 바로 FDR(KRX 단독) 값으로
  폴백**한다 — `J`로 다시 KIS를 호출해도 FDR과 같은 KRX 단독 값일 뿐이라(직접 검증함),
  UN에 데이터가 있는 종목만 KIS를 쓰고 없는 종목은 KIS를 추가로 부르지 않는다(불필요한
  API 호출·호출 제한 소모 방지, 2026-06-21 결정).
- **등락률 기준선은 전일 UN 종가가 아니라 전일 KRX 단독(J) 종가**(2026-06-21 수정): 실제
  HTS 화면(SK하이닉스/삼성전자/삼성전기/SK스퀘어)과 대조해보니 현재가는 통합(UN) 최종가가
  맞는데 등락률/대비는 달랐다 — 예) SK하이닉스 06-19: HTS 대비 +70,000(전일 **J** 종가
  2,685,000 기준)인데, 전일 **UN** 종가(2,699,000) 기준으로 계산하면 +56,000이 나와서
  틀림. KRX의 공식 종가가 익일 가격제한폭 등 규제 기준이 되는 "전일종가"라서, NXT에서
  더 늦게 체결된 가격과 무관하게 등락률 기준선은 항상 KRX 단독 종가를 쓰는 것으로 보임.
  `fetch_kis_prev_close()`가 별도로 `J` 모드를 조회해 전일 종가를 가져옴 — 성공 종목당
  KIS 호출이 1회(UN)에서 2회(UN+J)로 늘어남.
- **호출 제한(`EGW00201`, "초당 거래건수를 초과하였습니다")**: 종목 사이 0.12초씩 쉬어도
  실제로 종종 걸림(직접 확인) — 걸리면 1초 쉬고 최대 3회 재시도한다.
- 종목 단위 호출 실패(상장정지·데이터 지연 등)는 그 종목만 FDR(KRX 단독) 값으로 폴백해서
  명단에서 빠지지 않게 한다. KIS 전체를 못 쓰면(키 없음·토큰 발급 실패) 기존 FDR 전용
  함수(`build_vol_list`/`build_rate_list`)로 통째로 폴백한다.
- 검증 결과(2026-06-19 기준, 180개 후보): UN에 데이터가 있는 133개는 KIS 성공·NXT
  미거래로 UN이 빈 47개는 FDR 폴백. SK하이닉스 거래대금이 FDR 단독 20.8조원 → KIS
  통합 34.7조원으로, 거래대금·등락률 상위 50 종목 구성 자체가 FDR 단독과 달라짐
  (NXT 비중이 큰 종목이 순위 상승).
- **한계**: 1단계 후보군 자체가 FDR(KRX 단독) 거래대금 30억(`RATE_PRECHECK_MIN_AMOUNT`)
  기준으로 뽑히므로, 이보다 더 작은 FDR 거래대금에서 극단적인 NXT 비중(2026-06-24 기준
  관찰된 최대치 +823%보다 훨씬 큰 경우)을 가진 종목이 나오면 이론상 여전히 누락될 수 있다
  (전종목을 다 보강하지 않는 한 100% 보장은 불가능한 트레이드오프). 30억은 2026-06-24
  실제 누락 사례(가장 작은 게 37.4억) 기준으로 여유를 둔 값.
- 과거 5개 날짜(06-15~19)도 1회성 스크립트로 백필함(2026-06-21). FDR `StockListing`은
  "오늘" 스냅샷만 줘서 과거 날짜의 RATE_MIN_AMOUNT 기준 후보군을 새로 못 뽑으므로, **이미
  저장돼 있던 그 날짜의 거래대금·등락률 top50 합집합(날짜당 53~80개)을 후보로 재사용**해
  KIS로 재계산·재정렬했다. `prevRank`는 이번 백필 배치 내에서 재정렬된 직전 날짜 순위를
  다시 따라가도록 재계산(06-15만 예외 — 그 이전 날짜가 백필 범위 밖이라 기존 값 유지 불가,
  prevRank 없음). 상장주식수(시가총액 계산용)는 단기간에 거의 안 바뀌므로 오늘자 FDR
  값을 그대로 재사용. 06-15~17은 원래 `vol`이 50개가 아니라 30개만 저장돼 있던 기존
  이력 버그도 이번 백필로 50개로 메워짐. `CACHE_VERSION`은 4→5로 한 번 더 올렸다(데이터
  내용만 바뀐 경우도 캐시 무효화가 필요해서, 스팩 제거 때와 동일한 이유).
  → 위 "등락률 기준선" 수정 직후 같은 방식으로 한 번 더 재백필함(`CACHE_VERSION` 5→6).

---

## 데이터 구조 (JavaScript / Python 공통)

**vol 항목:**
```javascript
{ rank, prevRank, code, name, price, change, changeRate, volume, marketCap, tradingVolume, high60Rate }
// prevRank: null = 신규, sector 필드 없음 (WICS 제거됨)
// marketCap: 억원 단위, tradingVolume: 백만원 단위 (둘 다 2026-06-21부터, FDR 원 단위에서 변환)
// → 단위가 100배 차이나서 거래대금 표의 "거래대금/시가총액" 열(Tables.jsx VolTable의
//   `ratio = tradingVolume/marketCap`, 구 Cards.jsx CardA, 2026-06-22 표로 이동)은 둘을
//   그대로 나눈 값이라 실제 회전율의 100배로 부풀려진 숫자임 (의도된 동작 — 보정하지
//   않기로 결정함, 2026-06-21)
// high60Rate: 60일 신고가 대비 등락률(%), 0 이하만 나옴(0 = 신고가), null = 캔들 데이터 없음
//   (Tables.jsx 표에선 표시만 하고 volume은 더 이상 표 컬럼에 안 씀 — 필드 자체는 유지)
```

**rate 항목:**
```javascript
{ rank, code, name, price, change, changeRate, isUpperLimit, volume, high60Rate }
// contractStrength 필드 제거됨 (FDR 전환 시 더 이상 제공 불가)
```

**indices 필드** (stock_data 문서에 같이 저장됨, 일간):
```javascript
{ kospi: { close, change, changeRate }, kosdaq: { close, change, changeRate } }
// change = 포인트 변동(부호 포함), changeRate = %
```

**weekly_indices 컬렉션** (`_id`가 주차 키, 주간분석.py가 채움):
```javascript
{ _id: "2026-W25", kospi: { close, change, changeRate }, kosdaq: { close, change, changeRate } }
// close/change는 그 주 마지막 거래일 기준, changeRate는 전주 마지막 거래일 종가 대비 %
```

**localStorage 구조:**
```
analysis_dates       → JSON 배열 ["2026-06-17", ...]
analysis_YYYY-MM-DD  → JSON { vol:[...], rate:[...], date:"...", indices:{...}, _v: CACHE_VERSION }
```

> ⚠️ **stock_data 스키마를 바꿀 때마다 `src/utils.js`의 `CACHE_VERSION`을 올려야 한다.**
> 캐시에 `_v`가 같이 저장되고, `App.jsx`의 `loadAnalysis()`가 `_v !== CACHE_VERSION`이면
> localStorage를 무시하고 `/api/getData`에서 새로 받아온다. 안 올리면 이미 캐싱된 날짜는
> 새 필드(예: indices)가 영원히 안 보이는 버그가 생긴다 (2026-06-18 실제로 겪은 버그).
> 필드를 추가할 때만이 아니라 **기존 필드의 값 단위를 바꿀 때도 마찬가지** — marketCap/
> tradingVolume을 억원/백만원 단위로 바꾸면서(2026-06-21) `CACHE_VERSION`을 2→3으로 올림.
> 이미 MongoDB에 저장된 과거 문서들은 원 단위로 남아있던 2건(2026-06-18, 19)만 1회성
> 스크립트로 변환해 백필함 — 그보다 더 오래된 문서(06-15~17)는 신기하게도 이미 억원/백만원
> 단위였음(FDR 전환 전 HTS 엑셀 시절 단위가 그대로 남아있던 것으로 추정). 앞으로 다른 과거
> 날짜를 새로 채울 일이 있으면 단위가 섞여있을 수 있으니 값 크기(예: marketCap > 10억)로
> 변환 여부를 먼저 확인할 것.
> **필드 추가/단위 변경 같은 스키마 변화가 아니어도, MongoDB의 기존 문서 내용 자체를 고친
> 경우(예: 스팩 종목 제거 + rank 재정렬, 2026-06-21)에도 똑같이 올려야 한다** — 안 그러면
> 이미 옛 데이터를 캐싱해둔 브라우저는 MongoDB가 고쳐진 뒤에도 옛 localStorage 캐시를 그대로
> 보여준다(실제로 겪음: 스팩 제거 후에도 화면에 계속 보임 → `CACHE_VERSION` 3→4로 해결).

---

## AI 분석 JSON 형식 (Claude Code 생성)

```json
{
  "date": "2026-06-17",
  "analysis": {
    "테마": [
      { "테마": "반도체/AI", "주요종목": "SK하이닉스(+5%), SK스퀘어(+6%)", "핵심재료": "250만닉스 달성, GAM 슈퍼사이클" }
    ],
    "거래대금": [
      { "종목명": "SK하이닉스", "상승원인": "...", "테마섹터": "반도체/HBM/AI인프라" }
    ],
    "등락률": [
      { "종목명": "한솔테크닉스", "상승원인": "...", "테마섹터": "전선/전력인프라" }
    ]
  }
}
```

> ⚠️ **`테마` 배열을 쓸 때 지킬 규칙** (2026-06-22 정리, 프론트엔드가 이 배열을 표(테마/
> 핵심재료/주요종목 3열)로 렌더링하기 때문):
> - **`테마` 필드는 15자 이내 짧은 태그**로 작성(예: `반도체/AI`, `차익실현`, `LG/피지컬AI`).
>   단 줄인다고 회사·그룹명을 통째로 빼서 의미가 모호해지면 안 됨 — 예) `LG그룹/피지컬AI`를
>   `그룹/피지컬AI`로 줄이면 어느 그룹인지 알 수 없어지므로 금지, `LG/피지컬AI`처럼 식별자는
>   살리고 부수적인 글자만 줄인다.
> - **내용이 겹치는 테마는 하나로 합친다** — 예) "SK하이닉스 시총 1위" 이슈와 "반도체수출
>   188% 폭증에 따른 코스닥 소부장 강세"가 같은 날 같이 나오면 `반도체/시총1위` 하나로 묶고
>   주요종목에 대표 종목들을 같이 나열한다. `반도체/X`, `반도체/Y`처럼 같은 업종 접두어를 가진
>   항목을 여러 개 늘어놓지 않는다.
> - **`테마` 배열은 최대 6개까지만 작성한다**(2026-06-24, 사용자 요청 — `Analysis.jsx`의
>   `ThemeTable`도 `MAX_THEMES=6`으로 7번째부터 자르므로, 7개 이상 만들어도 화면엔 어차피 안
>   보인다). 후보가 6개를 넘으면 비중·등락률이 작거나 겹치는 테마부터 합치거나 제외한다.
> - **`핵심재료`는 50자 이내, 명사형으로 끝나는 구(句) 나열**(예: `SK하이닉스 시총1위 등극,
>   반도체수출 188% 폭증`) — `~했다`, `~입니다`, `~했습니다` 같은 문장형 종결을 쓰지 않고,
>   글자수 제한 안에서 문장(구)이 끊기지 않게 완결되도록 쓴다.
> - **`주요종목`은 `종목명(+%)` 형태만 쉼표로 나열**(예: `SK하이닉스(+7.1%), SK스퀘어(+19.4%)`)
>   — 괄호 안에 부가 설명을 넣지 않는다. 소수점은 한 자리까지만 표기.
> - **`거래대금`/`등락률` 배열은 `종목명`·`상승원인`·`테마섹터` 3개 필드만 작성**(2026-06-22
>   `한줄요약`·`트리거` 필드 표시 제거 — `Analysis.jsx`의 `AiCard`가 이제 `상승원인`만
>   렌더링). `상승원인`에는 "얼마에 마감"처럼 가격·마감만 적지 말고 구체적 촉발 사건/뉴스와
>   그 결과(시세·수급 반응)를 함께 자유 문장으로 담는다 — 옛 `트리거` 필드가 하던 역할을
>   `상승원인` 하나로 합친 것. `한줄요약`·`트리거` 필드는 더 이상 생성하지 않는다.